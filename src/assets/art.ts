import captainWren from "./captain-wren.jpg";
import finaleConvoy from "./finale-convoy.jpg";
import harborChart from "./harbor-chart.jpg";
import pip from "./pip.jpg";
import titleHarbor from "./title-harbor.jpg";

/** Resolved URLs for the Sky Dock art plates, bundled by Vite so the production build and dev server agree. */
export const art = {
  /** Captain Wren portrait, shown as the narrator avatar on Wren-narrated berths. */
  captainWren,
  /** Autumn Convoy arrival plate, shown on the victory screen. */
  finaleConvoy,
  /** Nimbus Reach harbor chart plate, used as the chapter map background. */
  harborChart,
  /** Pip portrait, shown as the narrator avatar on Pip-narrated berths. */
  pip,
  /** Golden-hour harbor plate, used as the title screen background. */
  titleHarbor,
};
